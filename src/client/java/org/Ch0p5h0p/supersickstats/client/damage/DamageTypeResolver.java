package org.Ch0p5h0p.supersickstats.client.damage;

import net.minecraft.entity.LivingEntity;
import net.minecraft.entity.damage.DamageSource;

public class DamageTypeResolver {
    /*public static DamageType fromSource(DamageSource src) {
        if (src.isProjectile()) return DamageType.PROJECTILE;
        if (src.isFire()) return DamageType.FIRE;
        if (src.isExplosive()) return DamageType.EXPLOSION;
        if (src.isMagic()) return DamageType.MAGIC;
        if (src.getAttacker() instanceof LivingEntity) return DamageType.MELEE;
        return DamageType.OTHER;
    }*/
}
